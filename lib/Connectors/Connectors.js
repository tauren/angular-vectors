'use strict';

angular.module('Connectors', ['Vectors'])

.constant('connectorsConfig', {
})

.controller('ConnectorsController', [
  '$scope',
  '$attrs',
  'connectorsConfig',
  function($scope, $attrs, connectorsConfig) {
  }
])

;